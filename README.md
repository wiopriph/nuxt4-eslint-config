# @wiopriph/nuxt4-eslint-config

ESLint flat config for Nuxt 4 + TypeScript 5.6+ + Node ≥ 20.

## Installation
```bash
pnpm add -D @wiopriph/nuxt4-eslint-config eslint typescript
```

## Usage
```js
// eslint.config.js
import wiopriph from '@wiopriph/nuxt4-eslint-config'
export default [...wiopriph]
```
